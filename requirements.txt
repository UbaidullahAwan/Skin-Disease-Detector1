cat > requirements.txt << EOF
streamlit==1.28.1
numpy==1.24.3
pillow==10.0.0
scikit-learn==1.3.0
EOF

git add requirements.txt
git commit -m "Update requirements with specific versions"
git push origin main